{
  "name": "stockbook",
  "version": "1.0.0",
  "private": true,
  "description": "Wholesale stock & sales tracking with WhatsApp-format reports (Express + SQLite via Sequelize + React)",
  "scripts": {
    "dev": "node server/local.js",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm install && npm run build",
    "start": "node server/local.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.22.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.8",
    "sqlite3": "^5.1.7"
  }
}
